# AGENTS.md — Rustama Codebase Reference

## Overview

Rustama is a terminal UI (TUI) client for Ollama and cloud AI models, written in Rust (edition 2024, v0.4.0). It provides a chat interface with streaming responses, markdown rendering, syntax highlighting, an agentic tool system, and an embedded terminal panel (a real PTY the model can drive with tools).

## Build, Test, Run

```bash
cargo build            # dev build
cargo build --release
cargo run
cargo test             # ~130 tests; some PTY/retry tests are slow (~60s total)
cargo test <name>      # e.g. cargo test menu_ / throbber / read_file / lsp_
# slow acid tests (ignored by default):
cargo test --release pty_acid -- --ignored --nocapture
cargo test --release lsp_real -- --ignored --nocapture   # needs rust-analyzer installed
```

Tests live in inline `#[cfg(test)]` modules at the bottom of each source file (no `tests/` dir). Some tests spawn real PTYs — don't run them inside Rustama's own embedded terminal tool unless necessary; prefer the `bash` tool. `cargo test` on this codebase takes ~60s (the `mock_server_429_flow` test alone is 60s) — plan timeouts accordingly. LSP tests use a hermetic in-process mock server; only the ignored `lsp_real_rust_analyzer` acid test spawns the real binary.

## Project Structure

```
Rustama/
├── Cargo.toml
├── src/
│   ├── main.rs              # Entry point, event loop, ALL rendering (~1940 lines)
│   ├── app.rs               # App state, input handling, networking, tools, sessions (~8200 lines)
│   ├── config.rs            # INI config parsing/saving, cloud models, pricing (~1330 lines)
│   ├── ui.rs                # Theme, Button, dialogs, MainMenu widget (~1670 lines)
│   ├── lsp.rs               # LSP client (rust-analyzer): process, framing, diagnostics (~1100 lines)
│   └── primary_selection.rs # X11 primary-selection clipboard provider (~400 lines)
├── LSP_PLAN.md              # Design record: LSP/rust-analyzer support (implemented)
├── SPLIT_PLAN.md            # Roadmap: split app.rs into modules (planned)
└── AGENTS.md                # this file
```

## Architecture

### Event Loop (`main.rs`)

- `crossterm` raw mode + alternate screen + mouse capture; panic hook restores the terminal.
- `run_app()` polls events every 50ms; every event goes through the single `handle_event()` helper (first poll + `poll(ZERO)` drain loop). **Keyboard/mouse are never blocked** — even while streaming or waiting to retry. Only *sending* is blocked while `app.is_loading` (both send paths show a `⚠ Request in progress…` status warning). **F7 "Stop"** interrupts the in-flight request: `App::stop_request()` raises the shared `request_cancel: Arc<AtomicBool>` (checked in the stream loop and `retry_countdown`) and instantly clears `is_loading`, committing partial text with a `⏹ Stopped by user` marker.
- All rendering on the main thread with `ratatui`. `ui(f, app)` lays out: menu bar, output+status+input (optionally split with the terminal panel), hint bar, keybar, then dialog overlays.
- Streaming chunks arrive via `mpsc::channel`, polled with `try_recv()` in `app.check_responses()` / `check_model_responses()` each loop iteration (same pattern for the num_ctx auto-detect in `check_ctx_response()`).
- The embedded PTY's terminal queries (cursor position, DA1) are answered by the reader thread the moment it parses them (immediate, independent of the UI loop); `app.terminal_state.flush_replies()` on the UI loop is only a belt-and-braces net. Without fast replies, crossterm-based children (Rustama itself) abort at startup on their 2s `ESC[6n` window.

### Application Logic (`app.rs`) — the big one

Key types: `App` (all state), `ChatMessage` (User/Assistant/System/App/Thinking/FileContent/ToolCall/ToolResult), `InputMode` (Normal/Input/Menu), `Focus` (Output/Input/Terminal), `StreamChunk` (Text/Thinking/StatusTick/Stats/Truncated/Done/Error/ToolCalls), `TokenStats`.

- **Input**: `handle_global_key()` is the entry point; dispatches to Normal-mode nav, `handle_input_key()` (textarea, Enter sends, Alt+Enter newline), or Menu mode. Terminal focus forwards raw keys to the PTY via `key_to_pty_bytes()` (F8/^G close the terminal). F9 opens the menu (also: click, or Tab in Normal mode); F8 is the terminal toggle.
- **Slash commands** in `handle_slash_command()` (~line 4050): `/help /model|/use /list /tools /config /system /setsystem /log /maxrounds /temp /topp /topk /fpen /ppen /effort /maxtokens /seed /ctx|/context /proxy /session new|save|rename|load /workspace /justify /status /usage|/cost /quit|/q|/exit`. Slash-command parameter changes are session-only; the Settings dialog (F9 → Settings) persists via `save_config()` / `save_model_params()`.
- **Networking**: `send_to_ollama_async_with()` and `send_tool_results_async()` spawn background OS threads, each with its own single-threaded tokio runtime; `send_with_retry()` handles retries with `retry_countdown()` StatusTick updates (`app.retrying` is only a status-bar flag now, it gates nothing). Streaming parses Ollama JSON lines or OpenAI SSE. Request bodies are built by `build_chat_body()` (cloud → OpenAI-style `max_tokens`/`reasoning_effort`; Ollama → `options.*` + top-level `think`).
- **System prompt**: `App::build_system_content()` = configured `system_prompt` (or the built-in agentic default) + the application-global **`~/.config/rustama/AGENTS.md`** appended under a `# AGENTS.md` heading (`config::load_agents_md()`; missing/empty file ignored).
- **Auto-continue**: `needs_continuation()` detects answers that end mid-thought (transitional "Let me run X:" with no tool call) and auto-sends "continue"; cap `MAX_AUTO_CONTINUES = 10` per turn.
- **Stop (F7)**: every request thread gets a `cancel: Arc<AtomicBool>` (stored in `app.request_cancel`); the stream loop and `retry_countdown` poll it, `send_with_retry` returns `SendOutcome::{Response,Error,Cancelled}`. `stop_request()` (F7, works with terminal focused too) drops `response_rx` so late chunks fail to send, commits partial text/thinking plus a "⏹ Stopped by user (F7)" App marker, and suppresses auto-continue/tool rounds for that turn.
- **Tools**: schemas in `get_tool_definitions()`; stateless execution in the free fn `execute_tool_call()`; text-fallback parsing in `parse_text_tool_calls()`. App-level dispatch goes through `App::execute_tool()` → `execute_terminal_tool()` / `execute_lsp_tool()` / `execute_tool_call()` (in that order). 18 tools: `read_file` (supports `offset`/`limit` line windows with `[showing lines X-Y of Z]` markers), `write_file`, `edit_file` (result includes a `unified_diff()` of the change; `render_output` styles tool-result lines via the per-tool `tool_result_line_style()` dispatcher — diff colors only for `edit_file`), `bash` (30s timeout, sudo blocked), `list_files`, `search_files`, `search_content`, `fetch_url`, `web_search`, `terminal_open/send/read/close`, `lsp_diagnostics`, `go_to_definition`, `lsp_hover`, `lsp_references`, `lsp_completion`. Tool loop capped by `max_tool_rounds` (default 10).
- **Embedded terminal**: `TerminalState` (portable-pty + vt100). Reader thread feeds a vt100 `Parser` (screen model rendered cell-by-cell in `render_terminal_panel()`, colors via `vt100_style()`) and a raw `TerminalBuffer` (model-facing incremental cursor reads). `TerminalQueries` vt100 callback answers cursor-position/DA queries — the reader thread writes the reply immediately after parsing (primary path; `flush_replies()` on the UI loop is a safety net). PTY resizes with the panel; F8 opens/closes it ("Term"/"CloseTerm" keybar labels), F6/click focuses, F8/^G closes while focused, Ctrl+T hides. Rendering honors application-cursor-key mode (`application_cursor()`).
- **LSP client** (`src/lsp.rs`, `App::lsp`): rust-analyzer as a child process, `lsp-server` crate's `Message::read`/`Message::write` framing over the child's pipes (`Connection::stdio()` is server-oriented — not used). Reader thread routes responses to pending oneshots, stores pushed `publishDiagnostics` in a versioned `DiagStore`, and answers server→client requests with canned replies so rust-analyzer never stalls. Sync std-only concurrency (no tokio). `sync_file()` keeps didOpen/didChange in order; `lsp_diagnostics` prefers pull (`textDocument/diagnostic`, retries through rust-analyzer's "server cancelled the request" re-analysis window) and falls back to the push cache. stderr → `rustama-lsp.log`. `Drop`/`shutdown()` guarantee no orphan server. Lifecycle: started in `App::new` when `lsp = on`, restarted by `/workspace <dir>`. Auto-feedback: after successful `edit_file`/`write_file` on a `.rs` file, fresh diagnostics are appended to the tool result (`lsp_auto_diagnostics`, `App::maybe_append_lsp_diagnostics`). Position-based tools (`go_to_definition`, `lsp_hover`, `lsp_references`, `lsp_completion`) share the `lsp_position_args`/`lsp_position_preflight` helpers (1-based → 0-based conversion, server-status check + `sync_file`). **Scope gotcha:** only files inside the LSP workspace root (and part of the crate graph) get analyzed — e.g. a scratch file in `/tmp` returns "no diagnostics" even with a deliberate type error; for compile feedback on out-of-workspace files use `bash` + `rustc`/`cargo check`, or move the file inside the workspace.
- **Sessions**: JSON at `$XDG_DOCUMENTS/rustama/<id>.session.rustama` (`~/rustama` fallback). `save_session()`/`load_session()`/`autosave_session()`/`new_session()`.
- **Usage/cost**: `TokenStats`, `session_usage`, `format_token_stats()`, `session_cost()` (cloud pricing from config).

### Configuration (`config.rs`)

Hand-rolled INI parser (no external crate). Files:

- `~/.config/rustama/rustama.conf` — main config (`ollama_url`, `model`, `save_path`, `agentic`, `timeout_secs`, `logging`, `logfile`, `system_prompt`, `proxy`, `max_tool_rounds`, `max_retries`, `terminal_width_pct`, `justify`, `hintbar`, `lsp`, `lsp_server`, `lsp_workspace`, `lsp_auto_diagnostics`). Missing keys are backfilled with defaults on load.
- `~/.config/rustama/AGENTS.md` — app-global agent instructions appended to every system prompt (see above).
- `~/.config/rustama/cloud_models.conf` — per-cloud-model `api_url`/`api_key`/`api_model` + per-model params + optional pricing.
- `~/.config/rustama/model_params.conf` — Ollama per-model params: `[default]` base + `[model-name]` overrides; legacy global temp/top_p/top_k migrated into `[default]` on first run.
- `ModelParams`: temperature, top_p, top_k, frequency/presence penalties, `max_output_tokens`, `reasoning_effort`, `seed`, `num_ctx`.
- `update_ini_section()` rewrites one section in place, preserving comments and other sections — used by all `save_*` functions.
- Booleans accept English (true/yes/on/1) and Hungarian (igen).

### UI Widgets (`ui.rs`)

- `Theme` — all colors in one struct; `dark()` is the runtime preset, `default()` is `#[cfg(test)]`. Dialog colors mirror mc's "Configuration options" dialog: `dialog_bg` white, `dialog_fg`/`dialog_border` black, `dialog_title_fg` blue (title/hotkeys/emphasis), `dialog_focus_bg` cyan (focused rows/inputs/buttons), `dialog_input_fg` gray on cyan (unfocused inputs). Includes `menu_selected_bg/fg` (black/white) and `menu_unselected_bg/fg` (`MC_GREEN`/white). `pub const MC_GREEN` (turquoise `Rgb(0,187,187)`) is shared by the keybar and menu.
- `MainMenu` — menu bar + dropdowns. **Single source of truth**: `MENUS` order + `menu_title()`; `menu_spans()` computes column spans from title widths. `x_offset_for()`, `bar_col_to_menu()` (mouse hit-test), `next_menu()/prev_menu()`, and `render_bar()` all derive from it — never hardcode offsets. Render methods take `&Theme`.
- `Button`, `Dropdown`, `FileActionDialog` (composable builder: labels/inputs/dropdowns/file lists/buttons, `render()` + `hit_test()`), `MessageBox` (optional centered styled header via `with_header` — the About box uses `rustama_logo_lines()`), `ConfirmationBox`, `dialog_block()`.
- `FileListBox` — the reusable, mc-style file-list widget (entries + selection + scroll + focus): renders its own border (blue when focused), rows, and a scrollbar when the entries overflow (`FILE_LIST_MAX_VISIBLE` = 12 rows), and answers `hit_test(row, area)`. `FileActionDialog` embeds it via `DialogItem::FileList`; it is standalone, so any other dialog/screen can reuse it directly. Mouse wheel over the file dialog drives the selection bar (mc panel convention): `App::scroll_up`/`scroll_down` route to `file_dialog_select_prev`/`next` (shared with the Up/Down keys) when `show_file_dialog`.

### Rendering (`main.rs`)

- `render_output()` — chat history with multi-layer caching (`cached_output`/`cached_wrapped`/`cached_msg_count`/`cached_streaming_len`/`cached_width`); re-renders markdown only when history or width changes.
- `render_markdown()` — `pulldown-cmark` → styled `Line`s: headings, bold/italic, code blocks, tables (Unicode box-drawing), lists, rules, inline code. `normalize_code_fences()` fixes indented fences first.
- `highlight_code()` — `syntect` "base16-ocean.dark", lazy `OnceLock`.
- `wrap_and_justify_lines()` — wrapping + optional justification; code/table lines are never justified.
- `throbber()` — status-bar spinner; style picked **randomly per request** from `app::SPINNERS` (5 frame sets: braille circle/dots, quadrants, equalizer, trigrams) via `App::reroll_spinner()` (time-based PRNG, no rand crate); frame ticks on wall-clock every 100ms. Idle shows 🧘.
- `render_keybar()` — MC-style F-key strip, always the last line; hint bar above it only when `hintbar = on`.

## Dependencies (notable)

`ratatui 0.30` + `crossterm 0.29` + `ratatui-textarea 0.9`; `reqwest 0.12` (rustls) + `tokio 1.37` (background threads only); `pulldown-cmark 0.10`; `syntect 5` (default-fancy); `portable-pty 0.9` + `vt100 0.16`; `lsp-types 0.97` + `lsp-server 0.7` (LSP client; framing only — no `Connection`); `arboard 3` + `x11rb 0.14` (primary selection); `glob`, `regex`, `chrono`, `urlencoding`, `serde`/`serde_json`. Dead deps still in Cargo.toml: `dialoguer`, `marked`, `markdown`.

## Key Design Patterns

1. **Async via threads + mpsc** — sync main loop; HTTP on spawned threads with per-thread tokio runtimes; `try_recv()` polling.
2. **Dual API protocol** — one streaming infrastructure branches per request: Ollama native JSON vs OpenAI-compatible SSE.
3. **Render caching** — markdown/wrap recomputed only on change, not per frame.
4. **Never block the UI** — input always works; only sending is guarded (`is_loading`) with a user-visible warning.
5. **Single source of truth for layout-ish math** — e.g. menu spans computed from titles, not magic numbers. Keep it that way.

## Conventions & Gotchas

- **Style**: 4-space indent, `snake_case`, doc comments on public items, keep the `─── section ───` comment banners. Run `cargo build` (must be warning-free) and `cargo test` before declaring done.
- **`app.rs` is huge (~8300 lines)** — the known candidate for splitting; `SPLIT_PLAN.md` is the roadmap (see "Planned Features"). If a task touches a coherent chunk, extracting a module is welcome but not required.
- **Adding a tool** = 3 edits, all in app.rs: schema in `get_tool_definitions()`, arm in `execute_tool_call()`, and name in `TOOL_NAMES` (the unknown-tool error lists `TOOL_NAMES` directly, so no separate edit is needed). **Stateful tool** (needs `App`, like the terminal/LSP tools): instead of the `execute_tool_call()` arm, add an `execute_*_tool()` method returning `Option<String>` and chain it in `App::execute_tool()`.
- **Adding a slash command** = arm in `handle_slash_command()` + help text in `slash_help()`.
- **Adding a config key** = field in `Config` (+ its `Default` impl) + parse arm in `Config::parse()` + backfill entry in the `missing` list in `Config::load()` + a line in `Config::save()` (full-file rewrite). Add a Settings dialog field too if it should be user-editable.
- **Adding a menu item** = extend `item_names()` + `action_for()`; offsets/hit-testing are automatic (see `menu_spans()`).
- **The retry countdown tests and `mock_server_429_flow` are slow** (~60s); don't "fix" them for being slow.
- **Terminal tool escape sequences** are documented in a comment above `key_to_pty_bytes()`; TUI apps in the embedded terminal need application-cursor-key sequences (`ESC O A/B/C/D`), see `application_cursor()`. `terminal_send` writes input **verbatim** (no auto-Enter) — the model appends `\r` itself when it wants to submit a line.
- There are ~20 unit-test modules (`usage_tests`, `scrollbar_tests`, `truncate_tests`, `sessions_dir_tests`, `continuation_tests`, `menu_focus_tests`, `chat_body_tests`, `pricing_tests`, `throbber_tests`, …). New features should add tests to the matching module (or a new one).

## Recent Changes (keep this section current)

- **PTY terminal-query replies moved to the reader thread** (fix): Rustama launched inside its own embedded terminal hung at startup — crossterm's `Terminal::clear()` → `get_cursor_position()` sends `ESC[6n` and aborts after a 2s window, and the reply only ever flushed on the UI loop's 50ms `flush_replies()` cadence, which can be blocked for the whole window while tool calls execute on the main thread. Now the reader thread (which parses the child's output into the vt100 parser) writes pending query replies itself, immediately after parsing, via a shared writer handle that is installed **before** the thread starts; the writer is set into `self.writer` before spawn (was after). `flush_replies()` stays on the UI loop as a belt-and-braces net. Test: `pty_answers_cursor_query_without_ui_loop` — a bash child sends `ESC[6n` and reads the reply with a 3s timeout; the OK marker appears **without a single `flush_replies()` call** (passes in 0.15s).
- **About dialog ASCII-art logo (centered)**: the About box now renders the Rustama logo from `rustama.sh` (repo root) above the message, horizontally centered. New `rustama_logo_lines()` in ui.rs — the `echo -e` payloads converted to styled ratatui `Line`s preserving the ANSI color runs (red block letters `Color::Red`, light-red-on-red highlight blocks `Color::LightRed` on `Color::Red`, one bright-red accent, white separators). `MessageBox` grew an optional `header: Vec<Line>` (`with_header(...)`) — the layout widens the popup to fit the header and adds a blank separator row; `render()` centers each header line (`(inner.width - line.width)/2`). The About box call site: `MessageBox::new("About", ...).with_header(rustama_logo_lines())`. Tests: `logo_lines_match_rustama_sh_art` (the three art rows match the script's payloads exactly) + `about_renders_centered_logo` (TestBackend render: the art row sits below the top border with equal slack on both sides — note the char-vs-byte index trap with `find` on multi-byte UTF-8, solved with a char-position helper).
- **GitHub page in the About dialog**: the About box (Help → About / F9) now ends with `GitHub: https://github.com/walley/Rustama` (from the origin remote). The MessageBox word-wrap keeps the URL on a single unsplit line at normal terminal widths (inner width caps at 46 — the URL line is 45 chars). Tests: `about_dialog_tests` in main.rs (`about_message_contains_github_url`, `about_url_renders_unsplit` — renders the About box into a TestBackend and asserts the full URL appears on one line).
- **Settings dialog buttons moved inside the dialog** (fix): Save/Cancel drew **on the bottom border** of the Settings popup — `btn_y = inner.y + 13*2 + 1` overshot the inner area by one row (13 fields × 2 rows + spacer = the last inner row is `inner.y + 26`; the `+1` landed exactly on the border). Worse, the mouse hit-test in app.rs had **already drifted**: it assumed `dialog_h = 28` vs the renderer's 29, so clicks were tested against different geometry than what was drawn. New shared `settings_dialog_layout(area)` + `SETTINGS_DIALOG_W/H/FIELD_COUNT` in ui.rs (single source of truth — both `render_settings_dialog` in main.rs and `handle_settings_dialog_click` in app.rs go through it): buttons now sit on the **last row inside the dialog**, one blank row below the last field. Also noted while investigating: `render_load_dialog` has the same on-border pattern but is **dead code** — `show_load_dialog` is never set to `true` anywhere (verified across the full git history; the Load menu opens the FileActionDialog-based file picker instead), so the dialog can never appear. Tests: `settings_dialog_buttons_inside_border` (ui.rs geometry: btn row = inner+26, strictly above the border, below the last field) + `save_cancel_buttons_render_inside_dialog` (main.rs TestBackend render: border row intact with └┘ and no buttons; buttons on the row above).
- **Keybar field sizing fixed** (fills the width, near-equal fields, flush "10", F7 always visible): the old math counted "cells" per slot (2 for single digits, 3 for "10") so F10 got 1.5× everyone else (at w=120: 15 vs 10) plus a buggy `extra` distribution favoring the first fields, and long labels (`CloseTerm`) overflowed their slot without compensation — the strip underfilled the terminal. New `keybar_field_widths(labels, width)` (pure + testable, main.rs): every field starts at its even share (`width/10`, first `width%10` fields get the spare column) but never below its content minimum (2 number cells + label), then single-cell water-filling makes the sum exact — surplus to the smallest fields, deficits from the largest fields that still fit their content (if the terminal is narrower than the sum of minimums, fields stay at minimums and the strip clips). Rendering: keys 1–9 keep `" N"` (leading space + number) but `"10"` is flush at its field edge — no leading space — so all labels start at the same cell offset; labels pad (or truncate in absurdly narrow terminals) to fill their field exactly. F7 shows **"Stop" always** (previously only while a request was in flight, so the label flickered in/out between requests; idle F7 is the harmless "Nothing to stop" no-op — unchanged behavior). Tests: `keybar_tests` — exact fill from w=20 to w=500 for both F8 labels (with over-constrained minimums case), near-equality (w=80/"Term" → ten 8s; w=80/"CloseTerm" → 11 for F8, others within 1 cell of each other), narrow-terminal minimums, and a render test (row fully keybar-colored, " 1Help", "10Exit" flush — no " 10", " 7Stop" present while idle).

- **Send button: 15 chars + arrow, no border gap**: the input-box send button is now `[   send  ►   ]` — exactly 15 columns with a centered label and an mc-keybar-style `►` arrow — and sits flush against the input box's bottom-right `┘` corner. Fixed the "double space after the button": it came from rendering over `Button::width` (= display + the 2-col inter-button gap convention), which `Clear`-wiped two border columns past the `]`, punching a hole in the border line. The button now renders over exactly its display char count, so the border line resumes immediately after `]` (buttons have no shadow — the gap was just dead space). Also fixed a latent `Button::new` bug: width now counts **chars**, not UTF-8 bytes (a `►` is 3 bytes but 1 column; all existing ASCII dialog buttons unchanged). Send clicks are row-based in `handle_click` (whole bottom border row sends), so no hit-test change was needed. Tests: `send_button_tests` in main.rs (`button_width_counts_chars_not_bytes`, `send_button_is_15_chars_with_arrow` — renders over a `─` fill and asserts the line continues right after the button).
- **LSP diagnostics section renders magenta**: the auto-appended diagnostics block (everything from the `"Diagnostics: "` marker to the end of the result, added by `maybe_append_lsp_diagnostics` to `edit_file`/`write_file` results) now has its own color — magenta — visually separating compiler feedback from the diff. The render loop tracks the marker per result (`in_lsp_section`) and `tool_result_line_style(tool, line, lsp_section)` returns magenta for the section in any tool; the diff part keeps its +green/−red/@@cyan colors. Tests: `lsp_diagnostics_section_renders_magenta` (unit) + `lsp_section_colored_magenta_in_render` (TestBackend buffer-color assertions: diff cells Green/Red, diagnostics cells Magenta).
- **Tool results carry their call's #id**: `render_output` now numbers results the same way as calls — `#1 TOOL CALL: bash` is followed by `#1 RESULT: bash`. Pairing is by `tool_call_id` (a `HashMap` built while walking the calls), so results pushed out of order in a multi-call round still get the right number; id-less results (text-parsed calls) fall back to positional numbering. Tests: `tool_call_id_tests` in main.rs (paired numbering, out-of-order results pair by id, positional fallback).
- **Per-tool result coloring** (fix): the diff coloring (`+` green / `-` red / `@@` cyan) used to be applied to *every* tool's result — mis-coloring `read_file` output with Rust `+`-lines or Python `@decorator`s, `bash`/`lsp_diagnostics`/`search_content` output, etc. Now `render_output`'s ToolResult arm dispatches through a new `tool_result_line_style(tool, line)` in main.rs: diff colors only in the `edit_file` arm (its result embeds a `unified_diff()`); everything else renders plain dim lines. The dispatcher is the extension point — add a per-tool arm (plus its own `*_result_line_style` helper, like `edit_file_result_line_style`) when a tool needs its own result markup. Tests: `tool_result_tests` in main.rs (`edit_file_diff_lines_colored`, `other_tools_stay_plain` — covers the mis-coloring cases for 7 tools).
- **edit_file diffs render in full**: a second per-tool policy, `tool_result_renders_full(name)` (main.rs, next to the style dispatcher), bypasses the 500-byte preview and the 20-line cap for `edit_file` — the whole unified diff stays visible, no "... (N more lines)" note. Other tools keep the compact preview. Tests: `tool_result_full_tests` (TestBackend render of `render_output` — `edit_file` shows all 30 diff lines including the last, no note; `read_file` still cuts at 20 lines with the note).
- **File dialog: mouse wheel + scrollbars + reusable `FileListBox`**: the inline file-list rendering was extracted from `FileActionDialog` into a standalone `FileListBox` widget in ui.rs (`entries`/`selection`/`scroll`/`focused`; `geometry(available)` = the old `file_list_geometry` math, `natural_height()`, `render(f, area, theme)` drawing border + rows + scrollbar, `hit_test(row, area)`). `DialogItem::FileList` now holds the widget; the `add_file_list(...)` builder API is unchanged. When entries overflow the visible rows, the box reserves its last inner column for a ratatui `Scrollbar` (▲/▼ arrows, │ track, █ thumb — same convention as the output panel); rows shrink by one column so the selected-row highlight never hides the track. Mouse wheel over the open file dialog now drives the selection bar (mc panel convention): `scroll_up`/`scroll_down` route to the new `file_dialog_select_prev`/`next` helpers, shared with the Up/Down keys (`handle_file_dialog_key` refactored onto them); `adjust_scroll` uses the new `ui::FILE_LIST_MAX_VISIBLE` constant instead of a hardcoded 12. Tests: `file_dialog_tests` in app.rs (wheel drives selection+viewport, clamps at ends, ignored when closed, empty noop), ported geometry tests on the widget, `file_list_scrollbar_only_when_overflow`, and the smoke test now asserts track/arrows/thumb render and the highlight excludes the scrollbar column.
- **mc-style dialog colors**: all dialogs (`FileActionDialog`, `MessageBox`, `ConfirmationBox`, plus the model/load/save/settings dialogs in main.rs) now use the palette observed in mc's "Configuration options" dialog — white background, black text/frame, **blue** titles (`dialog_block` gained `title_style`; blue is also used for emphasis and the focused file-list border), **cyan** focus background (focused rows, dropdown selections, inputs, focused buttons; selected file-list rows = black on cyan via `list_selected_bg/fg`), and **gray-on-cyan** unfocused input fields (mc's `\u001b[90m` input look; focused inputs use black on cyan). New theme fields `dialog_fg`, `dialog_title_fg`, `dialog_focus_bg`, `dialog_input_fg`; removed the now-unused `focus_fg`/`accent`. All dialog call sites pass theme colors (no more hardcoded Green/Red dialog buttons); dialog text is `dialog_fg` everywhere (white-on-white bug prevented). Test: `theme_uses_mc_dialog_colors`.
- **LSP stretch tools** (`lsp_hover`, `lsp_references`, `lsp_completion`): complete the toolset from `LSP_PLAN.md`. New `LspClient` methods `hover`/`references`/`completion` follow the `goto_definition` request pattern (`request_value` + typed response parsing; hover handles all three `HoverContents` shapes via `format_hover_contents`, completion handles both `CompletionList` and bare-array shapes and renders `label — detail`). The app-level executors share new `App::lsp_position_args`/`lsp_position_preflight` helpers (arg validation, 1-based → 0-based, server status + `sync_file`), so `lsp_references` gained an optional `include_declaration` arg for free. The unknown-tool error now lists `TOOL_NAMES` directly (single source of truth) instead of a duplicated hardcoded list. Tests: hermetic mock-server handlers + `hover_returns_rendered_text`/`references_returns_locations`/`completion_returns_labels_with_detail` in lsp.rs, schema/stopped-server/arg-validation tests in app.rs, plus hover/references/completion checks in the `lsp_real_rust_analyzer` acid test (verified passing against rust-analyzer 1.94).
- **View menu → Hintbar**: new `View` submenu item "Hintbar" (below "Terminal") toggles the hint bar on/off at runtime (`MenuAction::ToggleHintbar`, flips `App::hintbar` in-memory, like Agentic Mode). Added `view_menu_maps_hintbar_action` test.
- **LSP support (rust-analyzer)**: new `src/lsp.rs` — `LspClient` spawns rust-analyzer, speaks JSON-RPC via the `lsp-server` crate's `Message::read`/`Message::write` framing over the child's pipes, reader thread routes responses/pushed diagnostics (std-only concurrency, no tokio). New `lsp_diagnostics` agentic tool (pull `textDocument/diagnostic` with retry through the "server cancelled" re-analysis window, push-cache fallback; `path`/`severity` args) — the model now gets real compiler feedback and can fix its own type errors. New `go_to_definition` agentic tool (`textDocument/definition`; `path`/`line`/`column` args, 1-based) resolves a symbol's definition and returns `file:line:col:` plus a source excerpt (`format_locations`), giving the model "go-to-def" navigation. Auto-feedback: successful `edit_file`/`write_file` on a `.rs` file appends fresh diagnostics to the tool result (`lsp_auto_diagnostics`, default on). New config keys `lsp` (default **off**), `lsp_server`, `lsp_workspace`, `lsp_auto_diagnostics`; new `/workspace <dir>|off` slash command (restarts the server on the new root). Tool dispatch unified in `App::execute_tool()` (terminal → LSP → stateless). Server stderr goes to `rustama-lsp.log`; `shutdown()`/`Drop` leave no orphans. Tests: hermetic mock server (in-process thread over `std::io::pipe()` pairs) + `lsp_real_rust_analyzer` acid test (ignored; run with `cargo test --release lsp_real -- --ignored --nocapture`).
- **Ollama context window (`num_ctx`)**: Rustama now overcomes Ollama's default 2048-token context limit by auto-detecting the model's native context window and sending it as `options.num_ctx`. Detection queries `/api/show` and reads `model_info.<architecture>.context_length` (falls back to `llama.context_length`); triggered on startup, on model switch, and via `/ctx auto`. The detected value is a per-run default that never overwrites an explicit setting. New `ModelParams.num_ctx` (config keys `num_ctx`/`n_ctx`/`context_length`, sent only for Ollama; cloud models ignore it). New slash command `/ctx <n> | auto | off` (+ `/context` alias); "Num Ctx:" field added to the Settings dialog (F9 → Settings persists it).
- `edit_file` tool result now embeds a unified diff (`unified_diff()`, LCS-based, ±3 context lines) instead of just "Successfully edited"; `render_output` colors diff lines in tool results (+green, −red, @@ cyan).
- F7 "Stop": interrupts the in-flight request (stream / tool round / retry countdown) — `App::stop_request()`, per-request `Arc<AtomicBool>` cancel flag, `SendOutcome` enum in `send_with_retry`. Partial text is kept with a stopped marker; the keybar shows "Stop" on F7 **always** (idle F7 is a harmless "Nothing to stop"); hintbar shows "F7:Stop" during requests.
- F8 is now the terminal toggle: opens+focuses the terminal when not running, closes it when running; ^G (was: release focus) also closes the terminal while focused. Keybar F8 label: "Term"/"CloseTerm"; hints live in the hintbar. F9 still opens the menu (keybar: "PullDn").
- `terminal_send` no longer appends a newline/Enter — input is written exactly as given; the model must include `\r` explicitly to submit a line (`TerminalState::send_input`, `pty_send_input_appends_nothing` test). Fixes batched arrow keys (`ESC O B` — printable bytes!) triggering Enter in TUI apps like mc.
- App-global `~/.config/rustama/AGENTS.md` loaded into every system prompt (`config::load_agents_md`, `App::build_system_content`).
- Keyboard no longer blocked during retry backoff; send attempts while busy show a warning (event dispatch unified in `handle_event()`).
- Random per-request throbber spinner (`SPINNERS`, `App::reroll_spinner`).
- `read_file` tool gained `offset`/`limit` line-window params.
- Menu colors moved into `Theme` (`menu_*` fields); menu bar offsets/hit-testing now computed from menu titles (`menu_spans()`).

## Planned Features (roadmaps)

Read the plan doc before starting a phase, follow its execution order, and update AGENTS.md as each phase lands — never document unimplemented features as if they existed. Run `cargo build` (warning-free) + full `cargo test` after each phase.

### Splitting `app.rs` — `SPLIT_PLAN.md` (PLANNED — no code moved yet)

The plan maps `app.rs`'s regions (types / terminal / tools / networking / tests) into sibling modules, enabled by multiple `impl App` blocks across files (fields are already mostly `pub`; no `lib.rs`, everything stays crate-internal). Phase 1 (free-standing code) is low-risk — do it first; move each region's inline tests with it.

### LSP support — `LSP_PLAN.md` (IMPLEMENTED)

Shipped as described in "Recent Changes" and the `app.rs`/`lsp.rs` bullets above; `LSP_PLAN.md` is kept as the design record (including deviations: `Connection::stdio()` not used, mock server is in-process). All stretch tools shipped (`lsp_definition` as `go_to_definition`, plus `lsp_hover`, `lsp_references`, `lsp_completion`). The only remaining follow-up from the plan is a diagnostics UI panel (Phase 4, was skipped).

## Notes for Future Development

- No trait abstractions or plugin system — adding tools requires editing `app.rs` directly.
- `app.rs` keeps growing — see `SPLIT_PLAN.md` before adding another large subsystem to it (LSP got its own `src/lsp.rs`; follow that precedent).
- Dead dependencies (`dialoguer`, `marked`, `markdown`) could be removed.
- No CI; `cargo test` is the gate.
